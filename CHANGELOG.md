# 更新记录

QuantDesk 的所有重要变更都会记录在此。
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.2.0] - 2026-09-19

### 新增

- **量化选股扫描档位扩展到 7 档**：50 / 100 / 150 / 200 / 300 / 500 / 1000 只（原先最大 150）
  - 档位定义在主进程（`SCAN_LIMITS`），前端通过 IPC 取用 —— 避免两处各硬编码一份导致「能选 1000、后端只认 300」
  - 界面显示每档的**预计耗时**与数据来源说明，点之前就知道要等多久
- **全市场分页拉取**（`datasource.rankAll`）
  - 实测东财 clist 每页**最多只返回 100 条**（传 `pz=200` 也只给 100），所以 1000 只要翻 10 页
  - 分页、去重、限流、进度回调一次性封装；单页失败不会丢掉前面已取到的页
  - 1000 只实测 **4.3 秒**拉完（11 页，去重后 1000 只）
- **动态限流**（`datasource.setThrottle`）
  - 按扫描规模自动调档：≤150 只用 3 并发/180ms；>600 只用 8 并发/80ms
  - 返回恢复函数，扫描结束自动复位 —— 不恢复的话一次激进扫描会把限流参数永久留在高并发档
- **扫描可取消**：扫到一半发现选错池子，点「取消」即停止，**已完成的部分结果保留**
- **结果分页**：每页 100 行。1000 行一次性塞进 DOM 会让滚动与列排序明显卡顿；
  排序与筛选仍作用于全量结果，导出 CSV 也是全量
- **候选池自动补齐**：内置池（如标普 500 权重股 70 只）不足以支撑所选档位时，
  自动用全市场成交额榜补齐，并在结果里标注「其中 N 只来自全市场补齐」。
  **不会**静默只扫 70 只却让你以为扫完了全市场
- 压测脚本 `scripts/scan-load.js`（`npm run scan-load -- 300`）：验证各档位在实际网络下的耗时与成功率

### 修复

- **全市场排行的 PE 字段取错**（严重）
  - `clist` 接口与 `stock/get` 接口的**同名字段含义不同**：`stock/get` 里 `f163` 是「市盈率×100」，
    而 `clist` 的 `f163` 是别的量 —— 实测返回 5.5 亿级数字（净利润量级）
  - 原来的取法 `pickFloat(f163, f164, f162)` 在 clist 上必然取到错值，
    使得「价值」因子在全市场扫描里给出与个股详情页完全相反的结论
  - 实测对照定位到正确字段 **`f114`**：NVDA 44.61 / MU 134.35 / AAPL 43.8 / MSFT 27.41 / TSLA 379.2，
    与 `quoteOne` 逐只完全一致
  - 该 bug 从 v1.0.2 起就存在，此前被「扫描后逐一补行情覆盖 PE」掩盖了；
    本次为提速改为直接使用排行数据，才暴露出来
- **全市场扫描的市值字段缺失**：`emRank` 未取流通市值，导致「规模」因子在排行榜扫描中失真

### 变更

- 行情补充策略优化：排行榜返回的行**自带市值与 PE**，直接使用，不再逐只补行情。
  1000 只扫描原本有一半时间耗在这一步；仅「内置池 / 自选」这类不带估值数据的候选才补，且限量
  （>300 只时不补，因子层按「缺失」处理 —— 宁可标为缺失，也不编造数据）
- 大结果集回传时裁剪非必要字段（1000 只全量回传有几 MB，IPC 序列化与渲染都会卡）

### 测试

- 冒烟测试从 239 项扩展到 **248 项**
- 新增覆盖：`rankAll` 分页与去重、翻到底识别、PE 口径一致性（与行情接口对照）、
  `setThrottle` 恢复函数与非法参数夹紧、小规模不产生额外分页开销
- 压测数据（本机实测，含降级重试）：
  - **300 只 18.4 秒**（排行 3.2s + K线 15.2s），成功率 99.3%
  - **1000 只 42.6 秒**（排行 7.9s + K线 34.7s），成功率 99.4%
  - 耗时预估公式按上述实测校准，偏差 ±30% 以内

## [1.1.0] - 2026-09-19

### 新增

- **机会雷达**（新视图 + 4 个新模块）
  - `src/shared/moonshot.js` —— 暴涨潜力评分（9 维度加权：弹性 22 / 压缩 16 / 量能 14 / 动量 14 / 位置 12 / 相对强度 8 / 空头燃料 8 / 催化 6 / 流通盘 6），输出 A/B/C/D 等级、**触发条件**、**失效条件**与**风险标记**
  - `src/shared/insight.js` —— 风险点与机会点引擎。**每条结论都必须带 `evidence`（触发它的原始数值）**，没有数据支撑的判断一律不输出
  - `src/shared/newsfeed.js` —— 新闻聚合、13 类主题分类与情绪打分（正负词库 + 3 天半衰期时效衰减）
  - `src/shared/events.js` —— 未来事件日历：官方宏观日程（FOMC / CPI / 非农 / PCE / GDP / PPI / JOLTS）、个股财报排期、分红拆股、期权到期与三重巫日
- **量化选股增强**
  - 因子从 6 个扩展到 **16 个**：新增趋势结构 / 突破强度 / 量能异动 / 波动弹性 / 波动压缩 / 均值回归 / 短期异动 / 回撤健康度 / 流动性 / 相对强度
  - 新增 **13 套预设策略**（动量核心 / 52周突破 / 放量启动 / 波动压缩待爆发 / 超卖反弹 / 小盘高弹性 / AI算力链轮动 / 低波质量 / 深度价值 / 财报前布局 / 相对强度领跑 / 防御配置 / 高弹性候选），每套都写明**交易逻辑**与**最容易亏在哪（watchOut）**
  - 扫描结果新增「因子视图」与「预设筛选摘要条」
- **新数据面**
  - 财报日历 / 分红 / 拆股（nasdaq calendar）
  - 空头持仓与回补天数（用于 squeeze 判断）
  - 分析师共识目标价与评级分布
  - 个股新闻（东方财富搜索）
- **命令行工具**
  - `scripts/radar-cli.js`（`npm run radar`）—— 与 GUI 共用同一份 datasource 与算法模块
  - `scripts/radar-report.js`（`npm run report`）—— 把多次扫描汇总成 HTML / Markdown 报告
  - `scripts/verify-quotes.js`（`npm run verify`）—— 多源行情交叉验证，防止脏数据静默污染
- **股票池扩充**：新增核电与铀 / 量子计算 / 生物科技 / 金融科技 / 加密相关 / 机器人自动化 / 国防航天 / 消费零售 / 高贝塔弹性 / 中小盘成长 / 高股息防御 共 11 个池子

### 修复

- **腾讯行情兜底源静默失效**：`qt.gtimg.cn` 只认 `usNVDA`（不带后缀），带 `.OQ` 会返回 `v_pv_none_match`。原实现只用了带后缀的写法，导致该源每次都失败但不报错。现改为两种写法依次尝试。
- **`events.dayStart()` 不支持时间戳入参**：`upcoming()` 默认传 `Date.now()`，导致 `from` 变成 `NaN`、所有日期比较为 false，**整个事件日历永远返回空且不报错**。已补上该分支并加回归测试。
- **`insight` 风险/机会分归一化错误**：原先用「实际命中项数的权重和」作分母，导致只有 1 条风险时该项被赋予 100% 权重，**单一风险压过三条机会**（明明在上升趋势里也判「偏空」）。改为固定分母。
- **`moonshot` 三个维度失去区分度**：`volume`（量比 0.8~1.0 被 clamp 到 0）、`position`（-25% 与 -55% 回撤同样给 50 分）、`float`（上界只到 400 亿，所有大票一律 0 分）—— 三项合计 26% 的权重形同虚设。已改为分段平滑映射。
- **新闻被泛市场噪音污染**：东财搜索是全文检索，搜代码会返回大量无关稿（搜 PLTR 能返回「智谱AI概念大爆发」）。新增**相关性过滤**（标题命中优先）与**句子级情绪**（先定位「提到这只股票的那句话」再打分）。
- **打包与发布脚本的版本号硬编码**：`build-dmg.sh` 与 `release.sh` 原先写死版本号，会导致「代码发新版、dmg 和 Release 还是旧版」。现统一从 `package.json` 推导。
- K 线数据源顺序调整：腾讯带后缀的 `usXXX.OQ/.N` 返回干净数组，不带后缀的会返回拼接脏数组，故优先使用前者。

### 变更

- 选股页 `dist` 脚本改为指向可用的 `scripts/build-dmg.sh`，原 `electron-builder` 路径保留为 `dist:builder`
- 渲染层自检（`--smoke`）新增 v1.1.0 断言：预设策略渲染、机会雷达视图、事件日历产出、风险机会引擎产出、暴涨雷达等级与失效条件、新闻情绪打分

### 测试

- 冒烟测试从 191 项扩展到 **239 项，全部通过**
- 新增回归测试：`dayStart` 时间戳入参、风险/机会分归一化、流动性否决降级、预设策略引用完整性

## [1.0.2] - 2026-09-13

### 新增

- **美股市场规则引擎**（`src/shared/market.js`）
  - 交易时段 / 假期 / 半日市 / EST–EDT 夏令时
  - T+1 结算（2024-05-28 起生效）
  - PDT（Pattern Day Trader）规则
  - SSR（做空提价规则）与 LULD（限价涨跌停价格带）
  - 市场熔断三级阈值
  - 真实美股费用模型：SEC Section 31、FINRA TAF、交易所费、清算费、CAT 费
  - 做空 / 盘前盘后 / 冰山 / TWAP / VWAP 等订单映射
- **绩效归因与税务模块**（`src/shared/perf.js`、`src/shared/tax.js`）
  - 年化收益 / 夏普 / 索提诺 / 卡玛 / 信息比率 / 最大回撤（含谷底、恢复天数、最长水下）
  - VaR / CVaR、Beta / Alpha / 跟踪误差
  - 归因：择时 / 选股 / 集中度 HHI
  - FIFO 回合配对、Wash Sale（洗售）检测、短期 / 长期资本利得
  - 1099-B / 1099-DIV / 1042-S 报税表归类、CSV 导出
- **风控中心**（`src/shared/risk.js`）
  - 账户级：日亏上限、最大回撤、总仓位、杠杆 / 购买力、PDT 提醒
  - 策略级：单笔风险、单票上限、行业集中度、因子暴露、Beta / VaR、财报日 / 黑名单
  - 做空专项：借券费率、挤空提示
  - 安全评分 100 分制，下单前预检给出「通过 / 警告 / 拦截」
- **模拟交易与撮合**（`src/shared/paper.js`）
  - 订单状态机：pending / submitted / partially_filled / filled / cancelled / rejected / expired
  - 市价 / 限价 / 止损 / 止损限价 / OCO / Bracket、TWAP / VWAP / 冰山
  - 部分成交、滑点、买卖价差、T+1 待结算队列
  - 对账（持仓 / 现金 / 订单状态差异分级）与上线就绪度 7 项清单
  - 盘前盘后规则：休市下单会被明确拒绝
- **券商适配与导出**（`src/shared/broker.js`）
  - IBKR / Alpaca / Tradier / TradeStation / Schwab / E*TRADE 六家映射
  - 订单字段差异抽象（`orderType` / `type` / `OrderType` / `priceType`）
  - 只做「校验 + 导出 curl」两步，**不代下单**
  - API Key 脱敏（尾 4 位）与断线重连策略
- **选股器增强**（`src/shared/screener.js`）
  - 标普 500 / 纳指 100 / 道指 30 / Russell 1000 / 热门美股 ETF / 自定义六类股票池
  - 动量 / 价值 / 质量 / 低波 / 规模 / 盈利修正 六因子评分
  - 参数扫描与多策略对比（支持显式排序指标）
- **监控告警系统**（`src/shared/alerts.js` + `src/main/notify.js`）
  - 16 种规则类型（价格 / 涨跌幅 / RSI / 成交量 / 日亏 / 回撤 / 仓位 / 波动率等）
  - 10 类异常检测（订单失败 / 数据断流 / API 断线 / 持仓不一致等）
  - 6 种推送渠道：Telegram、邮件、钉钉、企业微信、webhook、系统通知
  - 邮件 SMTP 支持 465 隐式 TLS 与 587 STARTTLS
  - 主进程轮询，窗口最小化不停止
- **界面视图**：模拟交易、风控中心、监控告警、税务复盘四个新页面
- **渲染层自检**：新增 `v1.0.2` 18 项断言，覆盖四个新视图与端到端往返

### 变更

- `store.js` 配置存储从浅展开改为深合并，避免修改一个阈值清空整组风控参数
- 回测页面新增基准选择、订单类型、费用口径（us/simple）、止损 / 止盈 / 移动止盈 / 最大持仓日、T+1 / PDT / SSR 约束开关
- 监控看板、异常列表、规则配置、推送渠道、审计日志统一到一个视图

### 修复

- `tax.js` 仅识别 `type` 字段，券商对账单用 `side` 时静默产出空报告
- `screener.js` 多策略对比结果未排序，摘要「最优」与实际表格首行不一致
- `paper.js` 市价单同时接收 `price`/`limitPrice`/`quote` 但只认前两个，导致行情字段传 `quote` 时报「价格无效」
- `paper.js` 幂等键包含市价单的实时报价，报价一跳即可绕过重复提交拦截
- `broker.js` 未校验 IBKR `conid`，发送到 Client Portal 会触发网关级晦涩错误
- `market.js` PDT 窗口内对字符串 / 对象日期归一不统一，可能把「已超限」误判为「仍有额度」
- `notify.js` SMTP AUTH LOGIN 少等一个 334，导致用户名 / 密码错位（看起来像密码错误）
- `notify.js` STARTTLS 升级后重复发起 STARTTLS 死循环
- `notify.js` 端口 465 硬编码 SSL 判定，非标 SSL 端口会走明文
- 风控等级文案与实现不一致（`pass/block` 实为 `ok/warn/danger`，评分方向说反）
- 监控看板金额格式化保留最多 3 位小数，整数感知的字段也显示多余小数位

### 验证

- 算法层自检 `npm run smoke`：**192 项全部通过**，新增 market / perf / tax / risk / paper / broker / screener / alerts 8 个模块
- 渲染层自检 `--smoke`：`errors: []`，K 线交互 18 项通过，v1.0.2 新视图 18 项通过
- 截图：`build/shots/` 共 19 张，覆盖新增四个视图
- DMG 打包：`hdiutil verify` checksum VALID

## [1.0.1] - 2026-09-12

### 新增

- **推荐买入 / 卖出区间**（`src/shared/levels.js`、`src/shared/tradeplan.js`）
  - 支撑压力位：摆动高低点聚类（聚类内触碰次数即强度）、经典枢轴点 P/R1–R3/S1–S3、
    斐波那契回撤 23.6% / 38.2% / 50% / 61.8% / 78.6%
  - 买入区间 = 前期支撑 ∪ 均线 ∪ 布林中轨 ∪ ATR 回调带，取交集最密集的一段
  - 卖出区间分三档减仓：T1 减 40% / T2 减 35% / T3 减 25%
  - 止损位 = 买入区间下沿再退 1 个 ATR，且必须落在第二支撑下方
  - 输出盈亏比与建议仓位
- **个股全面诊股**（`src/shared/diagnose.js`）
  - 六维打分：趋势 / 动量 / 量能 / 位置估值 / 波动 / 流动性
  - 机会清单与风险清单（风险带严重度等级）
  - 8 项体检 + 结论文本，**每条结论都挂可核对的数值依据**（如 `price 218.29 > MA200 197.1`）
- **个股期权策略**（`src/shared/options.js`）
  - Black-Scholes 欧式定价，正态分布 CDF/PDF 精确积分
  - 由 60 根历史波动率推算隐含波动率，输出 ±1σ 期望区间
  - 期权链展示 Delta 与 IV
  - 内置 10 种策略（铁鹰、备兑看涨、保护性看跌、牛市价差、熊市价差、跨式、宽跨式等），
    按适配度排序，给出最大盈亏、盈亏平衡点、胜率与资金占用
- 新增诊股 / 区间 / 期权三个界面视图
- 新增 `scripts/shots.sh` 批量截图脚本

### 变更

- **界面全面重做，对齐 Apple 开发者手册中 macOS 27 的 Liquid Glass 设计语言**
  - 毛玻璃质感：`backdrop-filter: blur(44px) saturate(1.8)` + 镜面高光 + 亮边缘描边
  - 统一窗口圆角与同心圆角、侧栏延伸至窗口边缘、滚动边缘效果
  - 深色 / 浅色 / 自动三态主题
- 周期切换（日 / 周 / 月）全链路打通，波动率与期权隐含波动率一律按所选周期年化
  （日线 252 / 周线 52 / 月线 12），避免口径混用导致的数值失真

### 修复

- **个股界面 K 线走势图控制崩溃**（用户反馈）
  - 现象：拖拽或缩放时抛 `Cannot read properties of undefined (reading 'low')`，静止不动则正常
  - 根因：可见区间算出小数下标（`start = 80.099`），`bars[80.099]` 为 `undefined`
  - 修复：可视区间恒为整数下标；缩放、十字光标命中、坐标轴三者共用同一个 `_range()`，
    消除两套算法漂移；贴最新时缩放锁定右边缘
- **市盈率整列显示 0.00**
  - 根因：东方财富美股行情取错字段（`f162` 恒为 0）
  - 修复：改由 `f163`（TTM）/ `f164`（动态）取值，并兼容 A 股 `f9` / `f115`；无数据时显示「—」
- **期权隐含波动率高达 250%**
  - 根因：年化系数硬编码 `√252`，周期切到月线后残留口径被放大
  - 修复：全链路传递周期参数，月线口径 IV 回落到合理区间

### 验证

- 算法层自检 `npm run smoke`：**74 项全部通过**
- 渲染层自检 `--smoke`：`errors: []`、K 线绘制像素 916301、新增面板全部正常渲染
- K 线交互自检 `SMOKE_CTRL`：**18 项全部通过**，覆盖极限缩放 30 连击、越界平移 ±999、
  「可视区间始终是整数下标」不变式断言
- 打包产物 `hdiutil verify` checksum VALID

## [1.0.0] - 2026-09-07

### 新增

- 首个正式版本：行情、分析、选股、回测、预警、板块六大模块
- 东方财富 → 腾讯 → 新浪三源降级行情链路
- macOS 应用打包（Apple Silicon 原生）
